# Ryovia Watch Page

UI-only copy of the Ryovia watch page for visual design work.

```bash
npm install
npm run dev
```

Open `http://localhost:3000`. The content and controls are static design fixtures.
